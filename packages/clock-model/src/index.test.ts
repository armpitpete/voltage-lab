import{describe,expect,it}from'vitest';import{bpmToHz,clockFrame,periodMs,pulseDurationMs,swungInterval}from'./index';describe('clock model',()=>{it('converts BPM to frequency',()=>expect(bpmToHz(120)).toBe(2));it('calculates a beat period',()=>expect(periodMs(120)).toBe(500));it('calculates gate duration',()=>expect(pulseDurationMs(120,.5)).toBe(250));it('distinguishes trigger from gate',()=>{expect(clockFrame(5,120,.5,2,2).trigger).toBe(true);expect(clockFrame(100,120,.5,2,2).gate).toBe(true);expect(clockFrame(300,120,.5,2,2).gate).toBe(false)});it('divides and multiplies deterministically',()=>{expect(clockFrame(0,120,.5,2,2).divided).toBe(true);expect(clockFrame(500,120,.5,2,2).divided).toBe(false);expect(clockFrame(250,120,.5,2,2).multiplied).toBe(true)});it('swings alternating intervals without changing their pair total',()=>{const a=swungInterval(500,.2,0),b=swungInterval(500,.2,1);expect(a).toBe(600);expect(b).toBe(400);expect(a+b).toBe(1000)})});
