export type SafeVoice={osc:OscillatorNode;gain:GainNode;pan:StereoPannerNode|null};
export function createSafeVoice(context:AudioContext,master:GainNode,panValue=0):SafeVoice{const osc=context.createOscillator(),gain=context.createGain();gain.gain.value=0;const pan=typeof context.createStereoPanner==='function'?context.createStereoPanner():null;if(pan){pan.pan.value=panValue;osc.connect(gain).connect(pan).connect(master)}else osc.connect(gain).connect(master);osc.type='sine';osc.start();return{osc,gain,pan}}
export async function stopAudio(context:AudioContext|null,master:GainNode|null,voices:SafeVoice[]){for(const voice of voices){try{voice.osc.stop()}catch{}voice.osc.disconnect();voice.gain.disconnect();voice.pan?.disconnect()}master?.disconnect();if(context)await context.close()}
