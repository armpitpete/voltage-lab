import{describe,expect,it}from'vitest';import{frequencyFromCv,harmonicAmplitudes,outputVoltage,peakToPeak,periodMs,phaseAt,waveformSample}from'./index';
describe('oscillator model',()=>{it('uses exact one volt per octave',()=>{expect(frequencyFromCv(220,1)).toBeCloseTo(440);expect(frequencyFromCv(220,-1)).toBeCloseTo(110)});it('applies cents accurately',()=>expect(frequencyFromCv(440,0,0,1200)).toBeCloseTo(880));it('converts frequency to period',()=>expect(periodMs(2)).toBeCloseTo(500));it('wraps phase',()=>expect(phaseAt(1.25,1)).toBeCloseTo(.25));it('produces bounded waveforms',()=>{for(const type of['sine','triangle','saw','square','pulse']as const)for(let i=0;i<100;i++)expect(Math.abs(waveformSample(type,i/100,.3))).toBeLessThanOrEqual(1)});it('applies amplitude and offset',()=>expect(outputVoltage(.5,4,1)).toBe(3));it('reports peak-to-peak voltage',()=>expect(peakToPeak(5)).toBe(10));it('models harmonic families',()=>{expect(harmonicAmplitudes('sine',4)).toEqual([1,0,0,0]);expect(harmonicAmplitudes('square',4)[1]).toBe(0);expect(harmonicAmplitudes('saw',2)[1]).toBeCloseTo(.5)})});
